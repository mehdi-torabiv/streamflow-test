import { createTheme, Theme } from '@mui/material';

const theme: Theme = createTheme({
  palette: {
    primary: {
      main: '#007bff',
    },
    secondary: {
      main: '#6610f2',
    },
  },
});

export default {
  theme,
};
